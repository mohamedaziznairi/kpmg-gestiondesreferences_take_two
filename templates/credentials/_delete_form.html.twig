<form method="post" action="{{ path('app_credentials_delete', {'referenceid': credential.referenceid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ credential.referenceid) }}">
    <button class="btn">Delete</button>
</form>
