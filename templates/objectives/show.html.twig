{% extends 'base.html.twig' %}

{% block title %}Objectives{% endblock %}

{% block body %}
    <h1>Objectives</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idobjectif</th>
                <td>{{ objective.idobjectif }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ objective.objectif }}</td>
            </tr>
            <tr>
                <th>ObjectifVf</th>
                <td>{{ objective.objectifVf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objectives_index') }}">back to list</a>

    <a href="{{ path('app_objectives_edit', {'idobjectif': objective.idobjectif}) }}">edit</a>

    {{ include('objectives/_delete_form.html.twig') }}
{% endblock %}
