{% extends 'base.html.twig' %}

{% block title %}Workstreams{% endblock %}

{% block body %}
    <h1>Workstreams</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idworkstream</th>
                <td>{{ workstream.idworkstream }}</td>
            </tr>
            <tr>
                <th>Workstream</th>
                <td>{{ workstream.workstream }}</td>
            </tr>
            <tr>
                <th>WorkstreamVf</th>
                <td>{{ workstream.workstreamVf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_workstreams_index') }}">back to list</a>

    <a href="{{ path('app_workstreams_edit', {'idworkstream': workstream.idworkstream}) }}">edit</a>

    {{ include('workstreams/_delete_form.html.twig') }}
{% endblock %}
